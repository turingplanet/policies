# THE ONE FLOW (constant #2): read manifest -> set up declared toolchain -> install + declared tests
# -> checks -> GATE DECIDES. The LLM review is advisory and structurally cannot gate (constant #3).
# Referenced live by members as @vN (REFERENCE path).
name: review-reusable

on:
  workflow_call:
    inputs:
      contract:
        description: >-
          Informational label for the manifest contract. The schema actually enforced is
          manifest.schema.json shipped in THIS policies version (the tag the member references),
          so the contract travels with the policies version. Reserved for future multi-contract support.
        type: string
        default: v1
      policies_ref:
        description: >-
          The policies @ref this member uses (e.g. v0.0.7) — MUST match the `uses: …@ref` line that
          invoked this workflow. Used to check out the contract schema + review agent at that exact
          version. GitHub does not expose the called reusable-workflow's own ref to the job
          (github.job_workflow_sha is empty here; GITHUB_WORKFLOW_REF is the *caller's* ref), so the
          version must be passed in. Unset → falls back to the policies default branch (legacy behavior).
        type: string
        required: false
        default: ""
    secrets:
      ANTHROPIC_API_KEY:
        description: Key for the advisory LLM review. Optional — absent on fork PRs; the agent skips.
        required: false

permissions:
  contents: read
  pull-requests: write   # lets the advisory agent post a PR comment (best-effort)

env:
  MANIFEST_PATH: agent.manifest.yaml   # the anchor — the one hardcoded path

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # 1) READ THE MANIFEST (the seam). The runner ships mikefarah yq; scalars print raw (no -r).
      - name: Read manifest
        id: m
        run: |
          test -f "$MANIFEST_PATH" || { echo "::error::no manifest at the anchor ($MANIFEST_PATH)"; exit 1; }
          {
            echo "language=$(yq '.toolchain.language'        "$MANIFEST_PATH")"
            echo "pm=$(yq       '.toolchain.packageManager'  "$MANIFEST_PATH")"
            echo "profile=$(yq  '.profile'                   "$MANIFEST_PATH")"
            echo "install=$(yq  '.commands.install'          "$MANIFEST_PATH")"
            echo "test=$(yq     '.commands.test'             "$MANIFEST_PATH")"
            echo "lint=$(yq     '.commands.lint     // ""'   "$MANIFEST_PATH")"
            echo "security=$(yq '.commands.security // ""'   "$MANIFEST_PATH")"
          } >> "$GITHUB_OUTPUT"

      # 2) CHECK OUT POLICIES AT THIS VERSION — the platform owns the contract schema AND the agent,
      #    both versioned with the ref the member referenced. So members never ship their own schema
      #    and can't drift the contract. This is a HARD step: the schema feeds the gate below.
      #    The version comes from the `policies_ref` INPUT (the member's review.yml passes it to match
      #    its `uses: …@ref`). GitHub does NOT expose the called workflow's own ref to the job:
      #    github.job_workflow_sha is empty here, and GITHUB_WORKFLOW_REF is the *caller's* ref. If the
      #    input is unset, checkout falls back to the policies default branch (legacy behavior).
      - name: Check out policies (contract schema + review agent)
        uses: actions/checkout@v4
        with:
          repository: turingplanet/policies
          ref: ${{ inputs.policies_ref }}
          path: .policies
          persist-credentials: false

      # 3) SET UP DECLARED TOOLCHAIN — driven by the manifest, not hardcoded per repo.
      - name: Set up Python
        if: steps.m.outputs.language == 'python'
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Set up poetry
        if: steps.m.outputs.pm == 'poetry'
        run: pipx install poetry

      # ---- HARD CHECKS ---- these FAIL the build. The gate weighs exactly these.
      - name: Contract check — manifest matches the platform schema
        run: |
          yq -o=json '.' "$MANIFEST_PATH" > /tmp/manifest.json
          npx --yes ajv-cli@5 validate --spec=draft2020 -s .policies/manifest.schema.json -d /tmp/manifest.json

      - name: Install (declared)
        run: ${{ steps.m.outputs.install }}

      - name: Tests (declared)
        run: ${{ steps.m.outputs.test }}

      - name: Lint (declared, if present)
        if: steps.m.outputs.lint != ''
        run: ${{ steps.m.outputs.lint }}

      - name: Security scan (declared, if present)
        if: steps.m.outputs.security != ''
        run: ${{ steps.m.outputs.security }}

      # ---- ADVISORY ---- the standing review agent (already checked out at .policies). This step is
      # continue-on-error, so the agent REPORTS but the gate DECIDES (constant #3).
      - name: LLM code review (advisory only — never gates)
        continue-on-error: true
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          REPO: ${{ github.repository }}
        run: |
          if [ -f .policies/agent/review.sh ]; then
            bash .policies/agent/review.sh "$MANIFEST_PATH"
          else
            echo "review agent unavailable — advisory step skipped" >> "$GITHUB_STEP_SUMMARY"
          fi

  # THE GATE = this job succeeding (every hard-check step above passed). The advisory step is
  # continue-on-error, so the agent can never flip the result.
